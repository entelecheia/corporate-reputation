defaults:
  - absa

use_task_as_initial_object: true
steps:
  - uses: pipe1
    with:
      dataset_path: datasets/processed/kakao
      verbose: true
  - uses: pipe4
    with:
      num_samples: 100
      randomize: false
      verbose: true
    verbose: true
  - uses: pipe2
    with:
      tasks: [QUAD]
      absa_config_name: gpt35
      text_col: bodyText
      batch_size: 2
      num_workers: 50
      remove_columns: [lastModifiedDt, bodyText, writers, serviceUrl]
      load_from_cache_file: false
      verbose: true
  - uses: pipe3
    with:
      dataset_path: outputs/preds/kakao_quad_100_gpt35
    verbose: true
