defaults:
  - __init__
  - /pipe@pipe1: save_raw_dataset

use_task_as_initial_object: true
steps:
  - uses: pipe1
    with:
      raw_dataset_dir: ${project.path.home}/workspace/datasets/raw/daum_news_20230707
      dataset_path: ${task.tak_root}/${task.path.dirnames.datasets}/processed/daum_news_20230707
      verbose: true
    verbose: true
